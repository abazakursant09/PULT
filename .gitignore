# ── Environment secrets ───────────────────────────────────────────────────────
.env
.env.*
!.env.example
!.env.*.example
*.local
!.env.local.example

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg
.eggs/
venv/
.venv/
env/
.env/
pip-log.txt

# ── Database files ────────────────────────────────────────────────────────────
*.db
*.sqlite
*.sqlite3

# ── Node / Next.js ────────────────────────────────────────────────────────────
node_modules/
.next/
out/
frontend/dist/
frontend/build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── IDE ───────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log
logs/

# ── Generated analysis / cache / backup artifacts ─────────────────────────────
graphify-out/
*.zip
project_backup_*/
frontend/tsconfig.tsbuildinfo

# ── Seller uploads ────────────────────────────────────────────────────────────
# Transient: an uploaded CSV is consumed at confirm and deleted. Nothing here is source, and a
# seller's financial export must never reach the repository by an absent-minded `git add`.
backend/uploads/
# LEGAL-PRELAUNCH-C1 — explicit, recursive, case-covering belt-and-suspenders for the CSV
# artifacts specifically (the blanket rule above already covers the whole tree; these make the
# CSV intent legible and guard-checkable at any nesting depth).
backend/uploads/imports/**/*.csv
backend/uploads/imports/**/*.CSV

# ── SECURITY-2D-3E1B-3A backup artifacts / secrets (never commit) ─────────────
# Database dumps, ciphertext, manifests, restore temp, local backup secret files, and
# private age identities must never enter the repository.
*.dump
*.dump.age
*.age
*.pgdump
*-globals.sql
*.manifest.json
ops/backup/secrets/
*age-identity*
*.identity
restore-tmp/

# ── SECURITY-2D-3E1B-3B1 PITR artifacts / secrets (never commit) ──────────────
pgdata/
pg_wal/
*.pgdata
pgbackrest-repo/
*.pgbackrest
spool/
*cipher*pass*
*.s3-secret
recovery.signal
standby.signal
postgresql.auto.conf.pitr
ops/pitr/secrets/
*minio-ca*.key
