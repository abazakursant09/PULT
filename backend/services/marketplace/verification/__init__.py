"""Credential verification (Identity/Connection contour).

Deliberately NOT part of Advisory Runtime: verification reaches a marketplace with a
seller's secret, while the diagnosis contour reads only evidence already collected. A
future background re-verifier gets its own runner and its own audit table.
"""
