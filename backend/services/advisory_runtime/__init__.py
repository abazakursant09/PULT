"""Advisory Runtime (Phase-0 foundation).

Foundation layer ONLY: runtime contracts (RuntimeContext, ProducerResult) and the
declarative producer registry (ProducerSpec, ADVISORY_PRODUCERS). No scheduler tick,
no runner, no producer adapters, no execution — those land in later slices.
"""
